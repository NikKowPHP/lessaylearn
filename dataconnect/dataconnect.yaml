specVersion: "v1beta"
serviceId: "lessaylearn"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "lessaylearn-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
